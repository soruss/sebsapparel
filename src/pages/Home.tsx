import React from 'react';
import Hero from '../components/Hero';
import Features from '../components/Features';

const Home: React.FC = () => {
    return (
        <main>
            <Hero />
            <Features />
        </main>
    );
};

export default Home;
